////////////////////////////////////////////////////////////////////////////////
// Выполнение кода на сервере.
//  
////////////////////////////////////////////////////////////////////////////////
#Область ПрограммныйИнтерфейс

Функция КодПроверитьНаСервере(Код, СкрытыйКод) Экспорт
	
	Перем Задание, Параметры;
	                     
	Попытка
		Выполнить("Если 1 = 2 Тогда " + СкрытыйКод + Символы.ПС + Код + Символы.ПС + "КонецЕсли;");
		Возврат Истина;
	Исключение
		Возврат ОписаниеОшибки();
	КонецПопытки;
	
КонецФункции

//Функция ВыполнитьНаСервере(Код, Параметр = Неопределено).
//Параметры:
//		Код 	 = строка кода, если нужно вернуть значение, то в коде нужно присвоить его переменной "Результат".
//		Параметр = Произвольного типа параметр, которым можно оперировать в коде.
//
//Выполняет код на сервере (можно вызывать с клиента).
//
//Пример:
//		ДокСсылка = РедактированиеКодаСервер.ВыполнитьНаСервере("Результат = Документы.Задание.НайтиПоНомеру(Параметр), НомерДок);
//
Функция ВыполнитьНаСервере(Код, Параметр = Неопределено) Экспорт
	
	Результат = Неопределено;
	Выполнить(Код);
	Возврат Результат;
	
КонецФункции

Функция ВычислитьНаСервере(Код, Параметр = Неопределено) Экспорт
	
	Возврат ВыполнитьНаСервере("Результат = " + Код, Параметр);
	
КонецФункции

#КонецОбласти