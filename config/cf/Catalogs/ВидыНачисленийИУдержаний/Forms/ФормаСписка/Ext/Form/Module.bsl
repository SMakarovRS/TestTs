
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Перезаполнить(Команда)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ПерезаполнитьПослеВопроса", ЭтотОбъект);
	ПоказатьВопрос(ОписаниеОповещения, 
		НСтр("ru = 'Будут восстановлены первоначальные виды начислений и удержаний. Продолжить?'"),
		РежимДиалогаВопрос.ДаНет);
		
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ПерезаполнитьПослеВопроса(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьВидыНачисленийИУдержанийНаСервере();
	Элементы.Список.Обновить();
		
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьВидыНачисленийИУдержанийНаСервере()
	
	Справочники.ВидыНачисленийИУдержаний.ЗаполнитьВидыНачисленийИУдержаний();
	
КонецПроцедуры

#КонецОбласти

