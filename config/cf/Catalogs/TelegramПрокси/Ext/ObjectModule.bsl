
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

    Наименование = СтрШаблон(НСтр("ru = '%1://%2:%3'"), НРег(Строка(Протокол)), Сервер, Формат(Порт, "ЧРД=; ЧРГ=; ЧГ="));
	Если НЕ Ссылка.Пустая() Тогда
		Если Ссылка.Сервер <> Сервер ИЛИ Ссылка.Порт <> Порт ИЛИ Ссылка.Протокол <> Протокол Тогда
			TelegramСервер.ОчиститьИсториюПрокси(Ссылка);
		КонецЕсли;
	КонецЕсли;
    
КонецПроцедуры

#КонецОбласти

#КонецЕсли